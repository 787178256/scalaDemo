server:
  port: 7777
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: admin
    url: jdbc:mysql://localhost:3306/bootscala?useSSL=false
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql